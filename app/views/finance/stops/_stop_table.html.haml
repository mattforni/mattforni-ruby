%table
    %tr
        %th
            Symbol
        %th
            Percentage (%)
        %th
            Last Trade
        %th
            Stop Price
        %th
            Price Diff
        %th
            Quantity
        %th
            Highest Price
        %th
            Highest Time 
        %th
            Last Updated
    - stops.each do |stop|
        %tr
            %td
                = stop.symbol
            %td
                = stop.percentage
            %td
                = stop.last_trade.round(3)
            %td
                = stop.stop_price.round(3)
            %td{class: price_class(stop.price_diff)}
                = stop.price_diff.round(3)
            %td
                = alt_display stop.quantity
            %td
                = alt_display stop.highest_price
            %td
                = alt_display stop.highest_time
            %td
                = stop.updated_at

